export class Task {
    constructor(taskDescription, taskColor, taskCompleted) {
        this.TaskDescription = taskDescription;
        this.TaskColor = taskColor;
        this.TaskCompleted = taskCompleted;
    }
}